---

---

<!-- Botón WhatsApp Flotante Principal (Farin) -->
<a
  href="https://wa.me/573203748886?text=Hola,%20me%20gustaría%20solicitar%20información%20sobre%20sus%20servicios"
  target="_blank"
  class="fixed bottom-8 right-8 w-16 h-16 md:w-20 md:h-20 bg-green-500 hover:bg-green-600 text-white rounded-full flex items-center justify-center text-4xl md:text-5xl shadow-2xl hover:scale-110 transition-all duration-300 z-50 whatsapp-float group"
  aria-label="Contactar por WhatsApp"
>
  <span class="group-hover:scale-110 transition-transform">💬</span>

  <!-- Tooltip -->
  <div
    class="absolute right-full mr-4 bg-gray-900 text-white px-4 py-2 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none"
  >
    <div class="text-sm font-semibold">¡Escríbenos!</div>
    <div class="text-xs">Farid: 3203748886</div>
  </div>
</a>

<!-- Botón WhatsApp Secundario (Nohemi) -->

<style>
  .whatsapp-float {
    animation: pulse 2s infinite;
  }

  .whatsapp-secondary {
    animation: pulse 2s infinite 0.5s;
  }

  @keyframes pulse {
    0%,
    100% {
      box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.7);
    }
    50% {
      box-shadow: 0 0 0 20px rgba(34, 197, 94, 0);
    }
  }

  @media (max-width: 768px) {
    .whatsapp-float .absolute,
    .whatsapp-secondary .absolute {
      display: none;
    }
  }
</style>
